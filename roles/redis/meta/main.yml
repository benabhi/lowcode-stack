# ==============================================================================
# Role: redis - Metadata
# ==============================================================================

---
galaxy_info:
  author: lowcode-stack
  description: Redis cache deployment for lowcode-stack
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions:
        - noble
  galaxy_tags:
    - redis
    - cache
    - docker

dependencies:
  - role: common
  - role: geerlingguy.docker
