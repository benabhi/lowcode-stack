# ==============================================================================
# Role: windmill - Main Tasks
# ==============================================================================

---
- name: Include deployment tasks
  ansible.builtin.include_tasks: deploy.yml
  tags:
    - windmill
    - deploy
    - optional

- name: Include health check tasks
  ansible.builtin.include_tasks: healthcheck.yml
  tags:
    - windmill
    - healthcheck
    - optional
