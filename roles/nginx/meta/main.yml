# ==============================================================================
# Role: nginx - Metadata
# ==============================================================================

---
galaxy_info:
  author: lowcode-stack
  description: Nginx reverse proxy with SSL/Let's Encrypt for lowcode-stack
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions:
        - noble
  galaxy_tags:
    - nginx
    - ssl
    - letsencrypt
    - reverseproxy
    - webserver

dependencies:
  - role: common
